# @trakrai/ptz-controller

Import `@trakrai/ptz-controller/styles.css` once from the consuming app entrypoint so Tailwind includes the shared PTZ classes consistently.

Shared PTZ controller hook and UI components built on the transport-backed device runtime context.
