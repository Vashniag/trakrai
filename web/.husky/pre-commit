if ! git diff --cached --quiet -- device/package-versions.json; then
  printf '%s\n' \
    'device/package-versions.json is generated by the device package pipeline.' \
    'Do not commit manual edits to this file.' \
    'Use device/scripts/manage_device_packages.py locally or let CI update it automatically.'
  exit 1
fi

pnpm -C web precommit
