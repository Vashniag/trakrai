# Local emulator dependency set for arm64 CPU environments.
# Keep the Jetson-pinned production/runtime set in `requirements.txt`.
#
# Goals here:
# - installable from public indexes on linux/arm64
# - compatible with Python 3.8 used by the CPU local emulator image
# - sufficient for import-time/runtime startup of `ai_inference`

numpy==1.24.4
opencv-python==4.10.0.84
redis==6.1.1

# Public arm64 CPU wheels available on PyPI.
torch==2.2.2
torchvision==0.17.2

matplotlib==3.7.5
pandas==2.0.3
# Ubuntu 18.04 arm64 emulator needs a manylinux2014 wheel; 10.4.0 resolves to manylinux_2_28.
Pillow==9.5.0
PyYAML==6.0.2
requests==2.32.4
scipy==1.10.1
seaborn==0.13.2
tqdm==4.67.1
